TELEGRAM_BOT_TOKEN="replace-with-a-real-bot-token-from-BotFather"
MINIAPP_URL="https://replace-with-the-deployed-miniapp-url.example.com"
BACKEND_API_BASE_URL="http://localhost:3000"

# Comma-separated Telegram user ids allowed to see the bot's admin panel
# (e.g. to add music tracks). Leave empty/unset to disable it entirely.
ADMIN_TELEGRAM_IDS=""
