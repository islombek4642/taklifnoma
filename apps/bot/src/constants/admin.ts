// Internal callback_data identifiers for the admin panel's inline buttons —
// not user-facing text (that comes from locales/), just wiring.
export const ADMIN_CALLBACKS = {
  ADD_MUSIC: "admin:add-music",
  CANCEL: "admin:cancel",
} as const;
